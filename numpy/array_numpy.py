#Normalmente se suele importar como np
import numpy as np

#Podemos crear un arreglo a partir de 
array = np.array([1, 2, 3, 4, 5])

# Y lo mostramos
##print(array) #Se mostraría como una lista, pero los valores no estarían separados por coma


#TIPO DEL ARRAY
type(array)

#Dimenciones del Array
array.ndim #Muestra filas y columnas. Ej: (2,5)


#TIPOS DE DATOS
array.dtype # Ej. dtype('int64')

